info:
  name: get all notes
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/allnotes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
